package de.medieninformatik.bibliothekxml;

/**
 * Utility-Klasse zum Validieren einer XML-Datei gegen ein XML-Schema
 */
public class SchemaValidator {

    /**
     * privater Konstruktor; Instanziierung von Utility-Klassen nicht nötig
     */
    private SchemaValidator(){}

    /**
     * Validiert eine XML-Datei gegen ein XML-Schema
     *
     * @params TODO: params
     * @return true, wenn die Validierung erfolgreich war, sonst false
     */
    public static boolean validate(/* TODO: params */) {
        return false; // TODO: implement validator
    }
}
